/**
 * Gunderscript Test Script
 * (C) 2014 Christian Gunderman
 */

function test(x, y) {
  return (x / y);
}

function exported main(hello) {
  // variable declarations
  // inline initializations don't work yet :(
  var x;
  //var y;

  // variable initializations
  //x = 1;
  //y = 2;

  // native function call
  //print(x + y);
  //x = (1 + 1) * 3;
  x = passthru(1) + 1;
  //print(passthru(x) + 1);
  //print(3 + 3);
  //print(test("Hello", "friend"));
  print(test(4, 2));
}